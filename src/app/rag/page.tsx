import { RagLabView } from "@/components/rag/RagLabView";

export const metadata = {
  title: "RAG lab — DrNote",
  description:
    "Test Trigger.dev exam-recall PDF extraction with region segmentation and three-layer question versions.",
};

export default function RagPage() {
  return <RagLabView />;
}
