name: Refresh catalog (deprecated)

# Superseded by catalog-sync.yml — keeps committed bundle out of CI.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use catalog-sync.yml instead. JSON bundle refresh is removed."
